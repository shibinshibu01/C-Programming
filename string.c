#include<stdio.h>

void printFun(char c);

int main(){

    //1
    // char arr[]={'S','H','I','\0'};
    // char arr1[]="BIN";
    // char arr2[10];
    // scanf("%s",arr2);
    // printf("%s%s %s",arr,arr1,arr2);

    //2
    // char arr[100];
    // //gets(arr);
    // fgets(arr,100,stdin);
    // puts(arr);

    //3
    char *str="Shibin";
    puts(str);
    str="Shibu";
    puts(str);
    return 0;
}
